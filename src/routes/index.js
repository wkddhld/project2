const signupRouter = require('./signupRouter');
const homeRouter = require('./homeRouter');
const productRouter = require('./productRouter');
module.exports = { signupRouter, homeRouter, productRouter };
